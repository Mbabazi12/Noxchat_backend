"use strict";
// createPoll(chatId: string, userId: string, data: CreatePollDto): Promise<Poll>
// vote(pollId: string, userId: string, optionIndexes: number[]): Promise<void>
// getResults(pollId: string, userId: string): Promise<PollResults>  -- respects anonymous + showResultsImmediately
// closePoll(pollId: string, userId: string): Promise<void>
// expirePolls(): Promise<void>  -- cron job
//# sourceMappingURL=service.js.map