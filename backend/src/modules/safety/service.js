// reportContent(reporterId, targetType, targetId, reason)
// checkAutoHide(targetId)            -- hide if 5 reports within 1 hour
// autoMuteSpammer(userId)            -- triggered by messageLimiter
// filterHateSpeech(text)             -- replace bad words with 🚫
// getReportsForModeration()          -- mod team only
// resolveReport(reportId, action)    -- mod team only
