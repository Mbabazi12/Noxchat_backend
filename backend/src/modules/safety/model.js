// Report schema fields:
// reporterId (ref: User), targetType (message|group|confession)
// targetId, reason, status (pending|reviewed|actioned)
// createdAt

// MutedUser schema fields:
// userId, mutedUntil: Date   -- auto-mute from spam detection
