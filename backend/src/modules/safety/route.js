const router = require('express').Router();

// POST   /api/safety/report
// GET    /api/safety/reports          -- mod only
// PATCH  /api/safety/reports/:reportId -- mod only

module.exports = router;
