// Echo schema fields:
// chatId (ref: Chat), senderId (ref: User)
// content, lockedMessageCount: Number (default 10)
// reactions [{ userId, emoji (✅|❓|🔥) }]
// expiresAt: Date   -- 24h after send
// active: Boolean
// createdAt
