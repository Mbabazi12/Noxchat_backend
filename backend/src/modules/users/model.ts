// Schema fields:
// name: string, email: string, password: string (hashed), dob: Date, avatar: string
// onlineStatus: boolean, lastSeen: Date
// ghostMode: { enabled: boolean, schedule: { start: string, end: string }, whitelist: ObjectId[] }
// noxCoins: number, dailyCoinLastClaimed: Date
// settings: { notifications: boolean, dataSaver: boolean }
// blockedUsers: ObjectId[], mutedUsers: ObjectId[]
// weeklyBadge: boolean, createdAt: Date
