// getProfile(userId: string): Promise<User>
// updateProfile(userId: string, data: Partial<User>): Promise<User>
// deleteAccount(userId: string): Promise<void>
// blockUser(userId: string, targetId: string): Promise<void>
// unblockUser(userId: string, targetId: string): Promise<void>
// muteUser(userId: string, targetId: string): Promise<void>
// setGhostMode(userId: string, options: GhostModeOptions): Promise<void>
// claimDailyCoins(userId: string): Promise<number>
// getLeaderboard(): Promise<LeaderboardEntry[]>
// getTransactionHistory(userId: string): Promise<Transaction[]>
