// Group schema fields:
// chatId (ref: Chat), name, photo, creatorId (ref: User)
// admins [userId], members [userId]
// confessionMode { enabled, votesFor [], votesAgainst [] }
// echoDisabled { until: Date }
// sleepModeSettings [{ userId, start, end }]
// reportedBy [{ userId, reason, createdAt }]
// createdAt
