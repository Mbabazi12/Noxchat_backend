// Status schema fields:
// userId (ref: User), type (text|image|video), content, mediaUrl
// reactions [{ userId, emoji (❤️|😂|😮|😢|🙏) }]
// viewers [{ userId, viewedAt }]
// hiddenFrom [userId]
// expiresAt: Date   -- 24h after post
// createdAt
