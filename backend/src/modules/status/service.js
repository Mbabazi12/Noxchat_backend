// createStatus(userId, data)
// getStatuses(viewerId)              -- returns non-expired, non-hidden statuses
// viewStatus(statusId, viewerId)
// reactToStatus(statusId, userId, emoji)
// replyToStatus(statusId, userId)    -- opens a DM
// getViewers(statusId, ownerId)
// hideFromUser(statusId, ownerId, targetUserId)
// deleteStatus(statusId, userId)
