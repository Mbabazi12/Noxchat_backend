// Confession schema fields:
// groupId (ref: Group), realSenderId (ref: User)   -- hidden from members
// content, upvotes [userId], downvotes [userId]
// reports [{ userId, reportedAt }], hidden: Boolean
// weeklyAward: Boolean
// createdAt
