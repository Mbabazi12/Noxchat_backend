// placeBet(sessionId, userId, amount)      -- validates min/max/balance
// settleBets(sessionId, winnerId)          -- transfers coins from losers to winner
// getTransactionHistory(userId)            -- last 30 days
// getLeaderboard()                         -- resets every Monday, top 3 get badge
