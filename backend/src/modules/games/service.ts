// createSession(type: GameType, chatId: string | null, creatorId: string): Promise<GameSession>
// joinSession(sessionId: string, userId: string): Promise<void>
// spectateSession(sessionId: string, userId: string): Promise<void>
// makeMove(sessionId: string, userId: string, move: unknown): Promise<GameSession>
// endSession(sessionId: string): Promise<void>
// getDailyChallenge(type: 'sudoku'|'wordle'): Promise<unknown>
// shareScore(sessionId: string, userId: string, chatId: string): Promise<void>
// getWeeklyStats(groupId: string): Promise<WeeklyStats>  -- for group awards
