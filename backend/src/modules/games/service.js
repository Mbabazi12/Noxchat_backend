// createSession(type, chatId, creatorId)
// joinSession(sessionId, userId)
// spectateSession(sessionId, userId)
// makeMove(sessionId, userId, move)
// endSession(sessionId)
// getDailyChallenge(type)             -- sudoku, wordle
// shareScore(sessionId, userId, chatId)
// getWeeklyStats(groupId)             -- for awards (champ = most wins)
