// sendMessage(chatId, senderId, data)          -- text, media, voice note
// getMessages(chatId, filters)                 -- by text, date, media
// deleteForEveryone(messageId, userId)         -- within 2-minute window
// deleteForMe(messageId, userId)
// pinMessage(chatId, messageId, userId)        -- max 3 pinned
// unpinMessage(chatId, messageId)
// markAsRead(chatId, userId)
// forwardMessage(messageId, toChatId, userId)
// getOrCreateDirect(userId, targetId)
// searchMessages(chatId, query)
// getThrowback(chatId, userId)                 -- random msg from 1 year ago
