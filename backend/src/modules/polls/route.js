const router = require('express').Router();

// POST   /api/polls/:chatId
// POST   /api/polls/:pollId/vote
// GET    /api/polls/:pollId/results
// PATCH  /api/polls/:pollId/close

module.exports = router;
