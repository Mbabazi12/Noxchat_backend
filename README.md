# Noxchat Backend — Phase 0 Skeleton

This repository contains a minimal infrastructure skeleton for Noxchat backend.

Quick start (from repo root):

```bash
docker compose up --build
# then open http://localhost:3000/health
```
